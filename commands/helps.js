module.exports= {
    name: 'help',
    needClient: true,
    description: 'A list of all the commands that are disposable to the user',
    execute(message, args) {

        const client = client;
    }
}